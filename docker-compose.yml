services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: proxy
      POSTGRES_PASSWORD: proxy
      POSTGRES_DB: proxy
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U proxy"]
      interval: 2s
      timeout: 3s
      retries: 20
